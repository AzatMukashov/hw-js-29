export interface IDish {
  id: string;
  name: string;
  email: string;
  urlImage: string;
  price: number;
}

export interface IDishMutation {
  name: string;
  email: string;
  urlImage: string;
  price: number;
}